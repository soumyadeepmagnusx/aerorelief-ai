# Optional: Google Gemini API Key for live multimodal drone damage triage & tactical incident commander.
# Note: AeroRelief AI includes a zero-key deterministic xBD-calibrated fallback engine out of the box,
# and judges can also enter an API key directly inside the UI modal at runtime.
VITE_GEMINI_API_KEY=your_google_gemini_api_key_here
